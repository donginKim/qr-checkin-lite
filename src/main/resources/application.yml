spring:
  datasource:
    url: jdbc:sqlite:./data/attendance.db
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

server:
  port: 8080
  forward-headers-strategy: framework
