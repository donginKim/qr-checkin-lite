# qr-checkin-lite-api

Spring Boot REST API for QR-based attendance.

- SQLite (single file)
- Spring JDBC (no JPA)
- Excel import/export
- QR generation

Run:
  ./gradlew bootRun

SQLite DB:
  ./data/attendance.db
